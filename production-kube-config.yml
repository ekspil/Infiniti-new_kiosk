apiVersion: apps/v1
kind: Deployment
metadata:
  name: terminal-front
  namespace: production
  labels:
    app: terminal-front
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 50%
      maxSurge: 50%
  selector:
    matchLabels:
      app: terminal-front
  template:
    metadata:
      namespace: production
      labels:
        app: terminal-front
    spec:
      containers:
      - name: terminal-front
        image: cr.yandex/crpbchff05ai3nceeln1/terminal-front:1.0.51
        imagePullPolicy: Always
        ports:
          - containerPort: 80
        readinessProbe:
          httpGet:
            path: /index.html
            port: 80
          initialDelaySeconds: 30
          periodSeconds: 5
          successThreshold: 1
          failureThreshold: 3
      imagePullSecrets:
      - name: gitlabregistrycred
